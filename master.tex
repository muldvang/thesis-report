%% Argumenter for Viterbi
% - Hvis man har flere modeller, men kun vil backtracke på den, som giver den
% største likelihood.
% - Viterbi-training.

%% Argumenter for indexed posterior decoding
% - If only part of e.g. a genome is of interest
% - If the posterior decoding is visualized interactively somehow


\documentclass[final,11pt,twoside,danish,english]{memoir}

% Math
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{amsfonts}
\allowdisplaybreaks{}           % Allow pagebreaks in math environments.
\makeatletter
\g@addto@macro\bfseries{\boldmath} % Typeset bold math in headings.
\makeatother

% Font
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{babel}
\usepackage{newpxtext}
\usepackage{newpxmath}
\usepackage{microtype}
\usepackage[autostyle=true,danish=quotes]{csquotes}
\MakeOuterQuote{"}
\usepackage{tket}

% Citations
\usepackage[round]{natbib}
\bibliographystyle{plainnat}

% Captions
\usepackage[small,hang,bf,margin=30pt]{caption}

% Graphics
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{float}
\usepackage{tikz}
\usepackage{pgfplots}
\usetikzlibrary{external}
\tikzexternalize[prefix=tikz/,mode=list and make]
\pgfkeys{/pgf/images/include external/.code=\includegraphics{#1}} % Makes
                                                                  % externalize
                                                                  % and draft
                                                                  % work
                                                                  % together.
\pgfplotsset{%
  invoke before crossref tikzpicture={\tikzexternaldisable},
  invoke after crossref tikzpicture={\tikzexternalenable},
  ylabel near ticks,
  legend columns=-1,
  legend style={at={(0.5, 1)},anchor=south,draw=none,fill=none,/tikz/every even
    column/.append style={column sep=10pt}},
  every y tick scale label/.style={at={(0,1)},anchor=east, font=\scriptsize,
    inner xsep=2pt, inner ysep=0pt},
  ytick placement tolerance=-2mm,
  max space between ticks=30pt,
}
\usetikzlibrary{arrows}
\usepgfplotslibrary{groupplots}

% Links
\usepackage[hidelinks]{hyperref}

% Fixme
\usepackage[draft]{fixme}

% Algorithms
\usepackage{algorithm}
\usepackage{algpseudocode}

% Check that all labels are referenced
\usepackage{refcheck}
\norefnames{}
\nocitenames{}

% Code listings
\usepackage{minted}
\usemintedstyle{default}

% Calc package
\usepackage{calc}

% Colors
\usepackage{color}
\definecolor{my-red}{HTML}{E41A1C}
\definecolor{my-green}{HTML}{4DAF4A}
\definecolor{my-blue}{HTML}{377EB8}
\definecolor{my-purple}{HTML}{984EA3}
\definecolor{my-orange}{HTML}{FF7F00}

% Section numbering depth
\setsecnumdepth{subsection}

% User defined macros
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}

% Adjust the amount of allowed whitespace between paragraphs.
% \setlength{\parskip}{0cm plus2mm minus0mm}

% Date
\usepackage{datetime}

% Named reference
\usepackage{nameref}

\begin{document}

\tikzset{%
    viterbi-l/.style={%
      only marks, mark=*, color=my-red
    },
    viterbi-p/.style={%
      only marks, mark=square*, color=my-green
    },
    viterbi-pm/.style={%
      only marks, mark=triangle*, color=my-blue
    },
    indexed-one/.style={%
      only marks, mark=*, color=my-red
    },
    indexed-many/.style={%
      only marks, mark=square*, color=my-green
    },
}

\frontmatter{}

\include{titling_page}

\include{abstract}

% \include{resume}

\include{acknowledgements}

\cleardoublepage{}
\tableofcontents

\mainmatter{}

\include{introduction}

\include{background}

\include{method}

\include{implementation}

\include{experiments}

\include{conclusion}

\appendix{}

\backmatter{}

\nocite{Tange2011a}
\bibliography{master}

\end{document}


%%% Local Variables:
%%% mode: latex
%%% TeX-command-extra-options: "-shell-escape"
%%% TeX-master: t
%%% End:
