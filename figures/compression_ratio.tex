\tikzsetnextfilename{compression_ratio}
\begin{tikzpicture}[trim axis left,trim axis right]
  \begin{groupplot}[
    group style={
      group name=my plots,
      group size=1 by 3,
      xlabels at=edge bottom,
      xticklabels at=edge bottom,
      vertical sep=0pt,
      every plot/.style={
        xmin=1024,
        enlargelimits=true
      }
    },
    xlabel=Sequence length,
    xmode=log,
    ]

    \nextgroupplot[ymode=log, height=40mm, width=80mm]

    \addplot[only marks, color=colorbrewer1, error bars, y dir=both, y explicit]
    table[x=T, y=compression_ratio, y error=compression_ratio_std]
    {plot_data/viterbi_fib_sequence_transformed.data};

    \addplot[only marks, color=colorbrewer4, error bars, y dir=both, y explicit]
    table[x=T, y=compression_ratio, y error=compression_ratio_std]
    {plot_data/viterbi_1_sequence_transformed.data};

    \legend{Fibonacci words, Unary}

    \nextgroupplot[height=40mm, width=80mm, ylabel=Compression ratio]

    \addplot[only marks, color=colorbrewer3, error bars, y dir=both, y explicit]
    table[x=T, skip coords between index={0}{1}, y=compression_ratio, y error=compression_ratio_std]
    {plot_data/viterbi_2_sequence_transformed.data};

    \legend{Random ($\lvert{\mathcal{O}}\rvert = 2$)}

    \nextgroupplot[height=40mm, width=80mm,]

    \addplot[only marks, color=colorbrewer2, error bars, y dir=both, y explicit]
    table[x=T, y=compression_ratio, y error=compression_ratio_std]
    {plot_data/viterbi_sequence_transformed.data};

    \legend{Random ($\lvert{\mathcal{O}}\rvert = 4$)}

  \end{groupplot}
\end{tikzpicture}
%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../master"
%%% End:
