\tikzsetnextfilename{viterbi_fib_pre_vs_running_many_path}
\begin{tikzpicture}[trim axis left,trim axis right]
  \begin{axis}[
    yticklabels={,,},
    scale only axis,
    legend style={at={(0, 1)},anchor=north west},
    stack plots=y,
    area style,
    enlarge x limits=false,
    enlarge y limits=false,
    xmode=log,
    ymin=0,
    ymax=1,
    width=\textwidth,
    ]

    \addplot[fill=gray, draw=gray, fill opacity=0.3]
    table[x=T, y=many_path_memory_pre_fraction]
    {plot_data/viterbi_fib_sequence_transformed.data} \closedcycle;

    \addplot[fill=my-blue, draw=my-blue, fill opacity=0.3]
    table[x=T, y=many_path_memory_running_fraction]
    {plot_data/viterbi_fib_sequence_transformed.data} \closedcycle;

    % \legend{Preprocessing, Viterbi};
  \end{axis}
\end{tikzpicture}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../master"
%%% End:
