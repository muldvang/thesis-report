\tikzsetnextfilename{assymptotic_viterbi_backtrack_k}
\begin{tikzpicture}[trim axis left,trim axis right]
  \begin{axis}[
    xlabel=Model size $N$,
    ylabel=$\frac{\text{Running time}}{N^2}$,
    xmode=log,
    ymode=log,
    ]
    \addplot[only marks, color=black, error bars, y dir=both, y explicit]
    table[x=N, y=many_path_backtrack_time, y error=many_path_backtrack_time_std]
    {plot_data/viterbi_model_transformed.data};

    \addplot[only marks, color=colorbrewer1, error bars, y dir=both, y explicit]
    table[x=N, y=many_path_backtrack_time/N^2, y error=many_path_backtrack_time/N^2_std]
    {plot_data/viterbi_model_transformed.data};

    \addplot[only marks, color=colorbrewer2, error bars, y dir=both, y explicit]
    table[x=N, y=many_path_memory_backtrack_time/N^2, y error=many_path_memory_backtrack_time/N^2_std]
    {plot_data/viterbi_model_transformed.data};

    \legend{Normal without division by $N^2$, Normal, Space saving};
  \end{axis}
\end{tikzpicture}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../master"
%%% End:
